import { HeaderDashboard } from '../../../components/Dashboard/HeaderDashboard';
import * as Styled from './styles';
import { SectionBackground } from '../../../components/SectionBackground';

export const DashboardCertificados = () => {
  return (
    <Styled.Container>
      <HeaderDashboard page="certificados">
        <SectionBackground> Certificadosssss</SectionBackground>
      </HeaderDashboard>
    </Styled.Container>
  );
};
