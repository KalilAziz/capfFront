import { HeaderDashboard } from '../../../components/Dashboard/HeaderDashboard';
import * as Styled from './styles';
import { SectionBackground } from '../../../components/SectionBackground';

export const DashboardProjetos = () => {
  return (
    <Styled.Container>
      <HeaderDashboard page="projetos">
        <SectionBackground> Projetossssss </SectionBackground>
      </HeaderDashboard>
    </Styled.Container>
  );
};
